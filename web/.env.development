# Committed on purpose: no secrets, and only read by `vite` (dev server), never by `vite build`.
# Supabase webhooks cannot reach localhost, so in dev the browser calls the routing endpoints
# itself. Production builds leave this unset and rely on the Database Webhooks.
VITE_ROUTING_TRIGGER=client
