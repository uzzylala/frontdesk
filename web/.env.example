VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only — bypasses RLS. Never prefix with VITE_ (that would ship it to
# the browser bundle). Used by web/server and web/api, never by web/src.
SUPABASE_SERVICE_ROLE_KEY=
